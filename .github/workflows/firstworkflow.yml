on:
  push:
    branches: main
  
jobs:
     runs-on: ubuntu-latest

steps:
      - name: Print the input name to STDOUT
      - run: echo hello world!
      - run: node --version
      - run: npm --version
